#include <stdio.h>

/**
*main-Entry point
*
*Description:A C program print number
*
*Return: Always 0 (success)
*/

int main(void)
{
int n = 0, n1 = 1;
while (n <= 8)
{
while (n1 <= 9)
{
if (n1 > n)
{
putchar(n + '0');
	putchar(n + '0');
if (n + n1 != 17)
{
break;
}
}
}
}
putchar(',');
putchar(' ');
n++;
n1++;
putchar('\n');
return (0);
}
